import { shorten, deleteCode, batchShorten } from "./shorten.controller.js";
import { redirect } from "./redirect.controller.js";

export default {
  shorten,
  deleteCode,
  batchShorten,
  redirect,
};
