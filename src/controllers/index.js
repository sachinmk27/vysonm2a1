import {
  shorten,
  deleteCode,
  batchShorten,
  editCode,
} from "./shorten.controller.js";
import { redirect } from "./redirect.controller.js";

export default {
  shorten,
  deleteCode,
  editCode,
  batchShorten,
  redirect,
};
